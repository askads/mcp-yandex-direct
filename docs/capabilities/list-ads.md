# Яндекс Директ: Список объявлений — MCP-инструмент (tool)

**MCP-инструмент (tool) для Яндекс Директа:** Возвращает список объявлений с необязательными фильтрами по кампании, группе, id, состоянию и статусу.

Техническое имя: `list_ads`

## Какую задачу решает

> Я хочу посмотреть объявлений.

Возвращает список объявлений с необязательными фильтрами по кампании, группе, id, состоянию и статусу.

## Когда использовать

Используйте эту возможность, когда нужен результат «Список объявлений» без ручной работы в интерфейсе Яндекс Директа. Операция выполняется только по вызову из AI-приложения.

## Что нужно передать

- `campaignIds` — **необязательно**. Фильтр по id кампаний.
- `adGroupIds` — **необязательно**. Фильтр по id групп объявлений.
- `ids` — **необязательно**. Фильтр по id объявлений.
- `states` — **необязательно**. Фильтр по состояниям объявлений.
- `statuses` — **необязательно**. Фильтр по статусам модерации.
- `fieldNames` — **необязательно**. Какие поля объявления вернуть.
- `limit` — **необязательно**. Максимум объектов на страницу.
- `offset` — **необязательно**. Смещение постраничной выдачи (сколько объектов пропустить).
- `autoPaginate` — **необязательно**. Забрать все страницы, идя по LimitedBy (limit тогда не ограничивает общий объём).

## Что вернёт

Возвращает список объявлений с необязательными фильтрами по кампании, группе, id, состоянию и статусу.

## Что изменится в Яндекс Директе

Инструмент только читает данные или состояние подключения и не изменяет их.

## Пример запроса

> Посмотреть объявлений в Яндекс Директе. Если не хватает обязательных идентификаторов, сначала уточни их.

## Возможные ошибки и ограничения

Проверьте обязательные параметры, права токена и актуальные ограничения исходного API.

Доступ также зависит от прав токена, квот и ограничений исходного API.

## Связанные MCP-инструменты

- [Действие с объявлениями](./ad-action.md) — `ad_action`
- [Создать текстовое объявление](./create-text-ad.md) — `create_text_ad`
- [Обновить текстовое объявление](./update-text-ad.md) — `update_text_ad`

## Технические сведения

- **Воздействие:** только чтение
- **Группа:** Объявления
- **Источник описания:** регистрация `list_ads` в `src/tools/ads.ts`
- [Полный технический справочник](../TOOLS.md)
- [Все MCP-возможности](./index.md)
